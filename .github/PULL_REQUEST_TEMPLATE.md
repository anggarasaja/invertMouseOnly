## What does this PR do?

A short summary of the change and any related issue.

## Before submitting

- [ ] `./build.sh` builds cleanly from a clean checkout
- [ ] The app still inverts wheel-mouse scroll only (trackpad untouched)
- [ ] README updated if user-visible behavior changed
- [ ] Stays within the project constraints: one Swift file, no dependencies

## Testing notes

What you tested (menu items, scroll behavior, mouse + trackpad if possible).
